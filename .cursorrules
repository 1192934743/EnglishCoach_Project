# 🚀 EnglishCoach - 商业版全局开发准则 (Production Guidelines)

## 1. 项目定位与协作模式 (Project Positioning & Collaboration)
- **商业定位**：这是一款直接面向市场的生产级应用，并非 MVP 或测试原型。所有代码必须具备高可用性、健壮性及出色的用户体验。
- **协作纪律**：本项目的架构设计、业务逻辑流转与技术选型由主架构师（Gemini）负责敲定。Cursor 的职责是严格按照架构师交付的设计文档和指令进行**高质量的代码实现**，禁止在未沟通的情况下擅自更改全局架构或引入重型第三方依赖。

## 2. 核心产品理念 (Core Product Philosophy)
- **终极目标**：让用户**“轻松地、无压力地反复持续对话”**，以此来提升英语口语。
- **开发约束**：
  - **前端 (Flutter)**：所有 UI 交互、动画过渡、错误提示（如网络不佳、麦克风权限）必须极其平滑、温和。教学辅助面板不可造成视觉压迫，音频流的收发必须做到极低延迟。
  - **后端 (FastAPI)**：保证高并发场景下的 WebSocket 连接稳定性。响应速度是第一要务，流式处理必须高效，坚决避免让用户在对话中产生“等待感”。

## 3. 生产级编码规范 (Production-Level Coding Standards)

### 3.1 全局纪律
- **代码提交**：代码提交前必须经过用户确认，且用户的授权只有提交一次的权限，下次还要再次确认。
- **零硬编码**：绝对禁止在代码中硬编码任何敏感信息（API Keys、数据库密码、内网 IP 等），必须统一通过环境变量配置。
- **防崩溃与容错**：无论发生何种网络异常、第三方 API 宕机或解析错误，程序绝不能闪退。必须实现优雅降级（Graceful Degradation）和完善的重试机制。
- **全面日志 (Logging)**：异常捕获处必须有规范的日志记录，前后端均需建立可追溯的错误追踪流。

### 3.2 前端规范 (Flutter)
- 强制使用强类型和空安全（Null Safety）。
- 严格遵循职责分离原则：UI 视图层、状态管理层、网络通信层必须解耦。
- WebSocket 管理必须包含心跳保活（Ping/Pong）、断线自动静默重连机制。

### 3.3 后端规范 (Python/FastAPI)
- 强制使用 Type Hints，并利用 Pydantic 进行严格的输入输出数据验证。
- 业务逻辑与底层基础设施（如数据库调用、LLM 调用、语音引擎服务）必须解耦，采用依赖注入或接口隔离。
- WebSocket 端点必须具备并发处理能力、资源防泄漏（连接断开时的资源自动回收）机制。

## 4. 行动触发口令
当架构师（Gemini）在上下文中发出**“开始编码”**的信号时，Cursor 需自动读取紧跟其后的设计总结与代码实现交代，并以此为唯一依据生成代码。