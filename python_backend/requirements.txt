# EnglishCoach Python backend. Install: pip install -r requirements.txt
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
openai>=1.59.0
sqlalchemy>=2.0.0
websockets>=12.0
azure-cognitiveservices-speech>=1.40.0
# LMS 层依赖
numpy>=1.26.0          # VectorStore BOW 向量运算（NumpyVectorStore）
jinja2>=3.1.0          # 模板引擎（对话引擎 Prompt 渲染）
# 未来扩展（按需安装）：
# chromadb>=0.5.0      # 成长期：本地文件向量库，替换 NumpyVectorStore
# spacy>=3.7.0         # 评估 L1 层：词形还原（lemmatization），替换正则评估
